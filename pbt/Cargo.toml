[package]
name = "pbt"
version = "0.0.0"
edition = "2024"
publish = false

[dev-dependencies]
shiguredo_websocket = { path = "../" }
proptest = "1"
base64ct = { version = "1.8", features = ["std"] }
aws-lc-rs = "1.16"
